1. add options for the user to select when running the pipeline
2. The user should have an option to skip all user interaction while executing
3. the user should have a choice of what snp caller they wish to use
